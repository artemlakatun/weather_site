/* document.addEventListener("DOMContentLoaded", function () {
    // Задержка перед началом закрытия черного экрана
    setTimeout(() => {
        const loader = document.querySelector('.loader');
        loader.style.animation = "none"; // Отменяем анимацию и оставляем черный экран статичным
        setTimeout(() => {
            loader.style.animation = "slideUp 0.5s forwards"; // Запускаем анимацию закрытия снизу вверх
        }, 1200); // 3 секунды (3000 миллисекунд)
    }, 0);
}); */